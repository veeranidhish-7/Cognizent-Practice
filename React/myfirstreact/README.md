# myfirstreact

Lab: Create a React app that prints "welcome to the first session of React" as a heading.

## How to run
1. Unzip this folder.
2. Open the folder in VS Code (or open a terminal inside it).
3. Install dependencies:
   npm install
4. Start the app:
   npm start
5. Browser opens automatically at http://localhost:3000
